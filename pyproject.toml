[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "asset-detection-bridge"
version = "1.0.0"
description = "AutoCAD Bridge client repo: pdf-markup-core + FastAPI bridge service (Windows + AutoCAD)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[tool.setuptools]
packages = []
